/**
 * Custom event name for programmatic triggering from external code (e.g., Syncfusion change handlers)
 * Usage: element.dispatchEvent(new CustomEvent('alis:trigger'))
 */
export const FORCE_TRIGGER_EVENT = 'alis:trigger';

